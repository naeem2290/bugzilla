<div class="row">
  <div class="col-lg-12">
    <div class="col-lg-7"><h1>Welcome to Manager</h1></div>
    <div class="col-lg-offset-8"><h1><%=link_to "logout", destroy_user_session_path, method: :delete   %><br></h1></div>
  </div>
</div>


<%=@user_typ%>
<h2>Projects</h2>
<div class="row">
  <div class="col-md-12">
    <div class="col-md-10">
      <table class="table table-bordered table-hover table-dark">
        <thead>
          <tr>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @projects.each do |project| %>
          <tr>
            <h2><td><%= project.title %></td></h2>
            <h4><td><%= link_to 'Show', project_path(project) %></td></h4>
            <h4><td><%= link_to 'Edit', edit_project_path(project) %></td></h4>
            <h4><td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td></h4>
            <h4><td><%= link_to 'Add Developers/QA', display_project_path(project) %> </td></h4>
            <h4><td><%= link_to 'Remove Developers/QA', delete_member_project_path(project) %> </td></h4>
          </tr>
        </tr>
        <% end %>
      </tbody>
    </table>
    </div>
  </diV>
</div>

<br>
<%= link_to 'New Project', new_project_path %>
